<!-- <li> -->
    <!-- <p> -->
        <!-- check if user logged in and the destroyer written the comment -->
        <!-- <% if(locals.user && locals.user.id == comment.user.id){ %>
            <small>
                <a href="/comments/destroy/<%= comment.id %>">X</a>
            </small>
        <% } %> -->
        
        <!-- <%= comment.content %>
        <br>
        <small>
            <%= comment.user.name %>
        </small>
    </p> -->
<!-- </li> -->

<!-- //for ajax -->
<li id="comment-<%= comment._id %>">
    <p>
        <% if (locals.user && locals.user.id == comment.user.id){ %>
        
        <small>
            <a class="delete-comment-button" href="/comments/destroy/<%= comment.id %>">X</a>
        </small>
        <% } %>
        <%= comment.content %>
        <br>
        <small>
            <%= comment.user.name %>
        </small>

        <!-- chnages :: display the likes of this comment, if the user is logged in, then show the link to toggle like 
        or else just show the like count -->
        <br>
            <!-- study about data attribute -->
        <small>

            <% if(locals.user){ %>
                <a class="toggle-like-button" data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%=comment._id%>&type=Comment">
                    <%= comment.likes.length %> Likes
                </a>
            <% }else{ %>
                <%= comment.likes.length %> Likes
            <% } %>

        </small>
    </p>    

</li>